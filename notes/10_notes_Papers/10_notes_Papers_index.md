# 10_notes_Papers Index

## Files

- Working-Note-Projection-Push-Back-Model-Balloon-Analogy.md — Working note projection push back model balloon analogy
    - depends on → > the source does not change — the **environment changes how it appears**
    - depends on → the state,
    - depends on → 3. **Single origin**
    - depends on → differences arise from response, not multiple sources
    - depends on → 4. **Emergent variation**
    - depends on → -----
    - depends on → So:
    - depends on → system moves toward balance
    - depends on → all behaviour comes from one source
    - depends on → its own state
    - depends on → the current state of (U)
    - depends on → the state is determined by the operator,
    - depends on → the solution must satisfy both simultaneously.
    - depends on → observed behaviour = result of their combination
    - depends on → 2. **Self-regulation**
- Working-note2-G-light-projection-push-back-and-DDPM-v2.md — Working note2 g light projection push back and ddpm v2
- DDF-P1-Conceptual-Foundations-and-Framing.md — Ddf p1 conceptual foundations and framing
- DDF-P1.1.md — Ddf p1.1
- DDF-P4.md — Ddf p4
    - depends on → the operator (L^\dagger L) remains well-defined
    - depends on → ( \psi )
    - depends on → However:
    - depends on → -----
    - depends on → feedback influences admissible states and solutions
- DDF-P2.md — Ddf p2
- DDF-P3.md — Ddf p3
    - depends on → -----
    - depends on → the domain and metric in which their variables are defined.
- DDF-P3.5.md — Ddf p3.5
    - depends on → the current observable state.
    - depends on → L_\psi
    - depends on → -----
    - depends on → representing the realised action of (L) under state-dependent constraints.
    - depends on → [
    - depends on → No physical laws or constants are derived in this paper.
    - depends on → We denote this as:
    - depends on → the current observable state ( \psi ).
    - depends on → ]

## Subfolders

- Papers/
